# Voodoo Whiskers – Maritime Infrastructure Watch

All publishable HTML and derived outputs live under `public/`. The repository may remain public and can later be deployed with Cloudflare Pages using build output directory `public`.

The module is an analyst-review aid. Proximity alone does not indicate hostile intent. Magic Paws may mirror the products for optional layers, ingest and downloads. Score integration remains disabled during calibration.
